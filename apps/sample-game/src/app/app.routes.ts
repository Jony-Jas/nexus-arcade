import { Route } from '@angular/router';
import { HelpComponent } from './remote-entry/help.component';

export const appRoutes: Route[] = [
  {
    path: 'help',
    component: HelpComponent,
  },
];
